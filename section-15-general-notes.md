# Section 15 - General Notes

Angular basically helps to convert your forms into an object which represent the current state. It has the values, the validity and many other informations.

_There are two approach:_

**Template Driven:** Angular automatically generates the form object from the DOM.

**Reactive:** we define the form object and connect it manually to the DOM - but in that way we have more control.
